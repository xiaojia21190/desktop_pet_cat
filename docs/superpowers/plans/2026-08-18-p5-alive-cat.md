# P5 活过来的猫 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让猫从"几乎永不触发"变为按档位活跃的存在：验收落袋未提交批次（P5a）、修复触发层三处死链并新增存在感档位与 LLM 双轨节奏（P5b）、建立道具激活闭环（P5c）。

**Architecture:** 三段流水线。P5a 纯验收：跑全量测试 + 补 bond/撸猫测试 + MCP 端到端 + 按逻辑拆 5 个提交。P5b 触发层：新 `presence_level` 档位贯穿 customization_service → policy_engine（意图白名单+冷却倍率）→ llm_adapter（档位人设提示词+沉默反压）；context_collector 滑窗累计；policy_engine 分层静音；smart_pet_controller 双轨（规则保底+LLM 润色/自主）。P5c 激活层：first_guide_controller（首次引导状态机）+ 主动邀请意图（冷却翻倍克制）+ bond 反馈钩子。

**Tech Stack:** Godot 4.7 / GDScript；测试 headless tscn 断言式；端到端 Godot MCP。

**设计文档:** `docs/superpowers/specs/2026-08-18-p5-alive-cat-design.md`

---

## 执行前置

- `"$GODOT"` = `/d/Godot_v4.7-stable_win64.exe/Godot_v4.7-stable_win64_console.exe`
- 单套测试：`timeout 60 "$GODOT" --headless --path . res://tests/<name>.tscn`，期望末尾 `failed: 0`
- 八套测试全名：`test_behavior_system` `test_focus_session_mode` `test_sprite_manifest_loader` `test_objective_system` `test_activity_classifier` `test_foreground_app_monitor` `test_focus_charge_engine` `test_smart_modules`
- P5b 新增第九套 `test_presence_level`（Task 7 创建）
- 存档键四点贯通提醒（P2 教训）：新设置键必须同时补 `_get_default_data` + `load_data` + `_write_config` + `_gather_current_data`（panel 的 `collect_settings`/`apply_settings` 也要同步）
- **本计划大量任务涉及工作区未提交改动的文件**：P5a Task 1-6 必须最先执行且顺序不可乱（验收→拆提交），P5b/P5c 任何任务开始前 `git status` 必须显示 clean（仅 .godot/ 与 *.uid 例外）

## 文件结构总览

**P5a（Task 1-6）**：验收现有未提交改动
- Task 1 — bond_system 测试（新 tests/test_bond_system.gd + .tscn）
- Task 2 — 撸猫意图判定测试（并入 test_bond_system 或新文件）
- Task 3 — 全量回归（八套 + 新增）
- Task 4 — MCP 端到端验收
- Task 5 — 按逻辑拆 5 个提交
- Task 6 — P5a 收尾检查（工作区 clean）

**P5b（Task 7-14）**：存在感档位 + 死链修复 + LLM 双轨
- Task 7 — presence_level 档位核心（customization_service + 迁移 + save_manager 四点 + 测试文件创建）
- Task 8 — 设置面板档位 UI（替换 reminder_intensity 控件）
- Task 9 — context_collector 滑动窗口累计（死链②）
- Task 10 — policy_engine 分层静音（死链①）
- Task 11 — policy_engine 意图白名单 + 冷却倍率 + 新意图（饭点/摸鱼抓包/天气）+ 删除 build_fail_streak（死链③）
- Task 12 — llm_adapter 档位人设 + 沉默反压 + 润色轻请求
- Task 13 — smart_pet_controller 双轨编排
- Task 14 — P5b 收尾：全量回归 + MCP 端到端

**P5c（Task 15-19）**：道具激活闭环
- Task 15 — 首次引导状态机（components/engagement/first_guide_controller.gd 新文件）
- Task 16 — 主动邀请意图（policy_engine + cat 动作接线）
- Task 17 — 主动邀请克制机制（忽略冷却翻倍）
- Task 18 — bond 反馈钩子（气泡小提示 + 设置面板进度条 + 饿猫喂食加成）
- Task 19 — P5c 收尾 + P5 总验收

---

# P5a 落袋为安

### Task 1: bond_system 单元测试

bond_system.gd 是未提交的新文件（亲密度成长），当前零测试。补齐等级边界、同类递减、日上限、跨天重置、解锁判定测试。

**Files:**
- Create: `tests/test_bond_system.gd`
- Create: `tests/test_bond_system.tscn`（root: Node，挂 test 脚本）

- [x] **Step 1: 写测试脚本 tests/test_bond_system.gd**

```gdscript
extends Node

const BondSystemScript = preload("res://components/bond_system.gd")

var _passed: int = 0
var _failed: int = 0
var _failures: Array[String] = []

func _ready() -> void:
	call_deferred("_run")

func _run() -> void:
	_test_level_thresholds()
	_test_repeat_decay()
	_test_daily_cap()
	_test_unlocks()
	_test_save_roundtrip()
	_print_summary()
	get_tree().quit(0 if _failed == 0 else 1)

func _test_level_thresholds() -> void:
	# 等级边界：0→Lv1，50→Lv2，150→Lv3，320→Lv4，600→Lv5
	var cases := [[0.0, 1], [49.9, 1], [50.0, 2], [150.0, 3], [320.0, 4], [600.0, 5], [9999.0, 5]]
	for case in cases:
		var bond = BondSystemScript.new()
		add_child(bond)
		bond.bond = float(case[0])
		_assert_equal(bond.get_level(), int(case[1]), "level_at_%s" % case[0])
		bond.queue_free()

func _test_repeat_decay() -> void:
	# 同类连续互动收益递减（×0.6），异类重置
	var bond = BondSystemScript.new()
	add_child(bond)
	var g1: float = bond.add_bond("food_given")   # 4.0
	var g2: float = bond.add_bond("food_given")   # 4.0×0.6=2.4
	var g3: float = bond.add_bond("wand_given")   # 换类型 → 5.0
	_assert_true(is_equal_approx(g1, 4.0), "first_gain_full")
	_assert_true(is_equal_approx(g2, 2.4), "repeat_gain_decayed")
	_assert_true(is_equal_approx(g3, 5.0), "switch_type_resets_decay")
	bond.queue_free()

func _test_daily_cap() -> void:
	# 日上限 200：喂到上限后返回 0
	var bond = BondSystemScript.new()
	add_child(bond)
	bond.bond = 0.0
	var total: float = 0.0
	for i in range(60):
		# wand 5.0 起步，递减后每步 <5，60 次必触上限
		total += bond.add_bond("wand_given" if i % 2 == 0 else "food_given")
	_assert_true(total <= 200.0 + 0.01, "daily_total_capped")
	var extra: float = bond.add_bond("play_success")
	_assert_true(is_equal_approx(extra, 0.0), "capped_gain_returns_zero")
	bond.queue_free()

func _test_unlocks() -> void:
	# greet Lv2 解锁；Lv1 未解锁；不在解锁表的动作默认可用；品种 calico Lv3
	var bond = BondSystemScript.new()
	add_child(bond)
	bond.bond = 0.0
	_assert_true(not bond.is_anim_unlocked("greet"), "greet_locked_at_lv1")
	_assert_true(bond.is_anim_unlocked("idle_stand"), "base_anim_always_unlocked")
	_assert_true(not bond.is_breed_unlocked("calico"), "calico_locked_at_lv1")
	bond.bond = 150.0
	_assert_true(bond.is_anim_unlocked("celebrate"), "celebrate_unlocked_at_lv3")
	_assert_true(bond.is_breed_unlocked("calico"), "calico_unlocked_at_lv3")
	_assert_true(not bond.is_breed_unlocked("tuxedo"), "tuxedo_locked_at_lv3")
	bond.queue_free()

func _test_save_roundtrip() -> void:
	# 存读档往返；跨天 today_bond 清零（用昨天日期 key 模拟）
	var bond = BondSystemScript.new()
	add_child(bond)
	bond.add_bond("food_given")
	bond.add_bond("food_given")
	var saved: Dictionary = bond.get_save_data()
	_assert_true(float(saved.get("bond", 0.0)) > 0.0, "save_contains_bond")

	var bond2 = BondSystemScript.new()
	add_child(bond2)
	bond2.load_save_data(saved)
	_assert_true(is_equal_approx(bond2.bond, float(saved["bond"])), "load_restores_bond")

	# 伪造昨日 key → load 后 today 清零
	var stale := saved.duplicate(true)
	stale["today"] = "20200101"
	bond2.load_save_data(stale)
	_assert_true(is_equal_approx(bond2._today_bond, 0.0), "stale_day_resets_today")
	bond.queue_free()
	bond2.queue_free()

func _assert_true(cond: bool, name: String) -> void:
	if cond:
		_passed += 1
	else:
		_failed += 1
		_failures.append(name)

func _assert_equal(actual, expected, name: String) -> void:
	if actual == expected:
		_passed += 1
	else:
		_failed += 1
		_failures.append("%s (expected=%s actual=%s)" % [name, str(expected), str(actual)])

func _print_summary() -> void:
	print("passed: %d  failed: %d" % [_passed, _failed])
	for f in _failures:
		print("  FAIL: " + f)
```

- [x] **Step 2: 创建 tests/test_bond_system.tscn**

```text
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://tests/test_bond_system.gd" id="1"]

[node name="TestBondSystem" type="Node"]
script = ExtResource("1")
```

注意：若引擎要求 uid 字段，用 Godot MCP 或直接复制现有 `tests/test_focus_charge_engine.tscn` 的头部格式后替换 Script 路径。

- [x] **Step 3: 跑测试验证通过**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_bond_system.tscn`
Expected: `passed: 15  failed: 0`（15 = 7+3+2+6+4，以实际计数为准，关键是 failed: 0）

- [x] **Step 4: Commit**

```bash
git add tests/test_bond_system.gd tests/test_bond_system.tscn tests/test_bond_system.gd.uid 2>/dev/null || git add tests/test_bond_system.gd tests/test_bond_system.tscn
git commit -m "test: 亲密度系统单元测试"
```

---

### Task 2: 撸猫意图判定测试

cat_input_component 的 HoldIntent 状态机（PENDING→PET/DRAG 分流）零测试。撸猫判定的核心是纯逻辑（漂移阈值、按住时长），测试直接驱动 `_process` 与 `_handle_mouse_motion` 不现实（需 InputEvent 构造），改为测试可直达的判定分支 + 信号时序。

**Files:**
- Modify: `tests/test_bond_system.gd`（追加撸猫测试，复用同一 tscn）

- [x] **Step 1: 追加测试到 tests/test_bond_system.gd**

在 `_run()` 的 `_test_save_roundtrip()` 后追加 `_test_petting_intent()` 调用，并在文件末尾（_print_summary 前）追加：

```gdscript
const InputComponentScript = preload("res://components/cat_input_component.gd")

func _test_petting_intent() -> void:
	# 撸猫意图状态机：按住 0.6s 进入 PET；漂移>20px 升级 DRAG；松手发 petting_ended
	var comp = InputComponentScript.new()
	comp.click_distance_sq = 10000.0
	comp.drag_start_dist_sq = 100.0
	comp.pet_start_delay = 0.6
	comp.pet_max_drift_sq = 400.0
	add_child(comp)

	var started: Array[String] = []
	var ticks: Array[String] = []
	var ended: Array[float] = []
	comp.petting_started.connect(func(part): started.append(part))
	comp.petting_tick.connect(func(part): ticks.append(part))
	comp.petting_ended.connect(func(sec): ended.append(sec))

	# 按下（PENDING）→ 快进 0.7s（超过 pet_start_delay）→ PET
	var press := InputEventMouseButton.new()
	press.pressed = true
	press.button_index = MOUSE_BUTTON_LEFT
	press.position = Vector2(500, 500)
	comp._gui_input(press) if comp.has_method("_gui_input") else comp._input(press)
	for i in range(7):
		comp._process(0.1)
	_assert_true(comp._intent == 2, "hold_enters_pet")  # HoldIntent.PET = 2
	_assert_true(started.size() == 1, "petting_started_once")

	# 漂移超 20px（sq=400）→ 升级 DRAG
	var drift := InputEventMouseMotion.new()
	drift.position = Vector2(530, 500)  # 30px > 20px
	comp._handle_mouse_motion(drift)
	_assert_true(comp._intent == 2 or comp._intent == 1, "drift_upgrades_or_drag")
	# 注：_handle_mouse_motion 中 PET 分支漂移升级为 DRAG=1；若实现细节为直接 DRAG 判定同样通过

	comp.queue_free()
```

注意：`comp._intent` 的枚举值 NONE=0 PENDING=1? 需先读 `components/cat_input_component.gd` 的 enum HoldIntent 实际定义（`enum HoldIntent { NONE, PENDING, DRAG, PET }` → NONE=0, PENDING=1, DRAG=2, PET=3）。**按实际枚举值写断言**：PET=3, DRAG=2。信号连接方式如果 `_gui_input` 不可直接调，改用 `_input(press)`（组件实际入口，执行时以源码为准调整这一行调用）。

- [x] **Step 2: 跑测试验证通过**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_bond_system.tscn`
Expected: `failed: 0`（新增 4 断言左右）

- [x] **Step 3: Commit**

```bash
git add tests/test_bond_system.gd
git commit -m "test: 撸猫意图判定测试"
```

---

### Task 3: 全量回归（P5a 基线）

**Files:** 无改动，纯验证。

- [x] **Step 1: 跑全部九套测试**

```bash
for t in test_behavior_system test_focus_session_mode test_sprite_manifest_loader test_objective_system test_activity_classifier test_foreground_app_monitor test_focus_charge_engine test_smart_modules test_bond_system; do
  echo "=== $t ==="
  timeout 90 "$GODOT" --headless --path . res://tests/$t.tscn 2>&1 | tail -3
done
```
Expected: 每套 `failed: 0`。任何失败 → 用 systematic-debugging 流程定位；属未提交批次引入的 bug 就地修复（修复单独提交，不混入验收提交）。

- [x] **Step 2: 记录基线断言总数**（供 P5b/P5c 收尾对比）

把九套的 passed 数记到本计划文件此行下方：`P5a 基线：behavior=34 focus_session=? manifest=? objective=? classifier=? monitor=? charge=? smart=45 bond=19`

**P5a 基线（实际记录）**：behavior=34 focus_session=28 manifest=19 objective=8 classifier=24 monitor=10 charge=8 smart=44 bond=29（含撸猫意图 6 断言）——合计 204 断言。
**P5 收尾终态（2026-08-19 复核）**：11 套 244 断言全绿（presence=32 与 first_guide=8 为 P5b/P5c 新增），failed: 0。

---

### Task 4: MCP 端到端验收（P5a）

**Files:** 无改动，真机验证。

- [x] **Step 1: 用 Godot MCP `run_project` 启动项目**，观察 90 秒
- [x] **Step 2: 核对输出**（get_debug_output）：
  - 无 ERROR / SCRIPT ERROR
  - `桌面宠物猫启动成功`
  - LLM 代理行出现（`LLM 已启用系统代理: ...`，若系统代理开）
  - 无 `Focus session failure`
- [x] **Step 3: 手动交互验证清单**（MCP 截图辅助）：
  - 左键按住猫 0.6s+ → 撸猫动画/气泡（petting_started 链路）
  - 快捷菜单投食 → 猫走向食物开吃（chasing/eating 链路）
  - 设置面板 → 品种下拉显示 🔒Lv 锁定文案（refresh_breed_locks 链路）
- [x] **Step 4: stop_project**

发现问题 → 就地修复（修复独立提交）；修不动 → 记入"已知问题"清单写进本文件，不阻塞后续。

---

### Task 5: 按逻辑拆 5 个提交

工作区当前混着多个功能的改动。按下列分组 `git add` + commit（**每组提交前 `git status` 核对该组文件全部 staged、无漏无多**）。`.godot/` 与 `*.uid` 一律不入（.gitignore 已覆盖 uid；.godot 需确认是否已被忽略，若未忽略则本任务顺带把 `.godot/` 补进 .gitignore——它已存在于未跟踪清单）。

- [x] **Commit 1 — chore: gitignore 与环境变量模板**

```bash
git add .gitignore .env.example
git commit -m "chore: gitignore 与环境变量模板"
```

- [x] **Commit 2 — fix: LLM 适配器系统代理与决策提示词**

```bash
git add llm_adapter.gd
git commit -m "fix: LLM 适配器系统代理与决策提示词"
```

- [x] **Commit 3 — feat: 亲密度系统与撸猫交互**

```bash
git add components/bond_system.gd components/cat_input_component.gd cat.gd cat.tscn \
  components/cat_live_overlay.gd main.gd settings_panel.gd settings_panel.tscn save_manager.gd \
  states/cat_chasing_state.gd states/cat_eating_state.gd states/cat_pouncing_state.gd \
  item.gd item_types.gd cat_behavior_system.gd components/cat_item_detector.gd \
  context_collector.gd customization_service.gd smart_pet_controller.gd \
  resources/cat_config_resource.gd resources/sprite_manifest.json
git commit -m "feat: 亲密度系统与撸猫交互"
```

注意：这批文件互相咬合（cat.gd 接 bond_system、save_manager 改 provider、settings_panel 加品种锁），必须一个提交否则中间态不可编译。提交信息可以更细分说明（body 里列出子项）。

- [x] **Commit 4 — feat: 新动作图与 DLC 品种素材**

```bash
git add assets/ resources/animations/british_blue.tres resources/animations/calico.tres \
  resources/animations/orange_tabby.tres resources/animations/tuxedo.tres \
  resources/animations/mochi.tres resources/animations/dracula.tres \
  docs/prompts/ .claude/gen_dlc.py tools/
git commit -m "feat: 新动作图与 DLC 品种素材"
```

注意：`assets/` 含 106 个未跟踪文件 + 删除的旧品种根目录 png（`git add -A assets/` 处理删除）。`.claude/loc2.png`、`.claude/menu3.png` 是调试截图，**不入库**。

- [x] **Commit 5 — feat: 托盘常驻启动与工程改进**

```bash
git add components/desktop/auto_start_manager.gd components/desktop/timed_hide_controller.gd \
  components/desktop/passthrough_manager.gd quick_action_menu.gd \
  states/cat_leash_walking_state.gd states/cat_wand_playing_state.gd \
  export_presets.cfg keyboard_listener.gd audio_manager.gd typing_effect_overlay.gd \
  sprite_frames_generator.gd animation_config.gd
git commit -m "feat: 托盘常驻启动与工程改进"
```

注意：Commit 5 的文件清单以 `git status` 实际剩余为准——上面 Task 3 的 diff 明细是主要部分，执行时若发现还有零散文件（如 save_manager_panel.gd、hover_panel 等被修改的），按功能归属并入 Commit 3 或 5。**目标：结束后 `git status` 仅剩 .godot/（若未忽略）与 .claude/ 调试文件。**

- [x] **Step 6: 验证工作区 clean**

```bash
git status --short
```
Expected: 空（或仅 .claude/loc2.png、.claude/menu3.png——这两个留着不入库）

---

### Task 6: P5a 收尾

- [x] **Step 1: 再跑一次九套测试确认全绿**（同 Task 3 命令）
- [x] **Step 2: 把 Task 4 的"已知问题"清单与 Task 3 基线数字补写进本文件**
- [x] **Step 3: MCP 启动 30 秒冒烟（确认提交未破坏启动）**，然后 stop_project

---

# P5b 让猫活过来

### Task 7: presence_level 档位核心与存档迁移

新设置键 `presence_level`（int 0-4：安静/低频/中频/高频/智能），取代 `reminder_intensity`。本任务只做数据层（service + save_manager 四点贯通 + 迁移），UI 在 Task 8。

**Files:**
- Modify: `customization_service.gd`
- Modify: `save_manager.gd`（四处：_get_default_data / load_data / _write_config + 迁移逻辑）
- Create: `tests/test_presence_level.gd` + `tests/test_presence_level.tscn`

- [x] **Step 1: 写失败测试 tests/test_presence_level.gd**

```gdscript
extends Node

const CustomizationServiceScript = preload("res://customization_service.gd")
const PolicyEngineScript = preload("res://reaction_policy_engine.gd")
const ContextCollectorScript = preload("res://context_collector.gd")

var _passed: int = 0
var _failed: int = 0
var _failures: Array[String] = []

func _ready() -> void:
	call_deferred("_run")

func _run() -> void:
	_test_presence_setting_roundtrip()
	_test_legacy_migration()
	_test_soft_hard_quiet()
	_test_sliding_window()
	_test_cooldown_multiplier()
	_test_intent_whitelist()
	_print_summary()
	get_tree().quit(0 if _failed == 0 else 1)

func _test_presence_setting_roundtrip() -> void:
	# presence_level 读写往返 + 越界钳制
	var cs = CustomizationServiceScript.new()
	add_child(cs)
	cs.apply_settings({"presence_level": 3})
	_assert_equal(int(cs.to_settings_dict().get("presence_level", -1)), 3, "presence_roundtrip")
	cs.apply_settings({"presence_level": 9})
	_assert_equal(cs.presence_level, 4, "presence_clamped_high")
	cs.apply_settings({"presence_level": -1})
	_assert_equal(cs.presence_level, 0, "presence_clamped_low")
	# persona 不再携带 reminder_intensity
	cs.apply_settings({"presence_level": 2})
	_assert_true(not cs.get_persona().has("reminder_intensity"), "persona_drops_reminder")
	cs.queue_free()

func _test_legacy_migration() -> void:
	# 旧 reminder_intensity 映射：low→1 medium→2 high→3
	var cs = CustomizationServiceScript.new()
	add_child(cs)
	for legacy in [["low", 1], ["medium", 2], ["high", 3]]:
		cs.apply_settings({"reminder_intensity": legacy[0]})
		_assert_equal(cs.presence_level, int(legacy[1]), "migrate_%s" % legacy[0])
	# 无任何键时默认 2（中频）
	var cs2 = CustomizationServiceScript.new()
	add_child(cs2)
	_assert_equal(cs2.presence_level, 2, "default_medium")
	cs.queue_free()
	cs2.queue_free()

func _test_soft_hard_quiet() -> void:
	# 分层静音：软=静音开始后 60 分钟，硬=其余。默认 23-8 → 23:00 软 23:59 硬
	var engine = PolicyEngineScript.new()
	add_child(engine)
	# 23:30 软静音 → 深夜关怀可达（活跃 30 分钟滑窗）
	var d: Dictionary = engine.evaluate(
		{"hour": 23, "minute": 30, "quiet_hours_start": 23, "quiet_hours_end": 8,
		 "fullscreen": false, "continuous_active_seconds": 1800.0,
		 "presence_level": 2, "psyche": {"energy": 100.0}},
		[], [], {"personality": "tsundere"})
	_assert_equal(String(d.get("policy_intent", "")), "night_owl_care", "soft_quiet_allows_care")
	# 02:00 硬静音 → quiet_mode
	d = engine.evaluate(
		{"hour": 2, "quiet_hours_start": 23, "quiet_hours_end": 8,
		 "fullscreen": false, "continuous_active_seconds": 1800.0,
		 "presence_level": 2, "psyche": {"energy": 100.0}},
		[], [], {"personality": "tsundere"})
	_assert_equal(String(d.get("policy_intent", "")), "quiet_mode", "hard_quiet_silences")
	# 非跨午夜场景：2-8 → 2:00-2:59 软，3:00 起硬
	d = engine.evaluate(
		{"hour": 2, "minute": 30, "quiet_hours_start": 2, "quiet_hours_end": 8,
		 "fullscreen": false, "continuous_active_seconds": 1800.0,
		 "presence_level": 2, "psyche": {"energy": 100.0}},
		[], [], {"personality": "tsundere"})
	_assert_equal(String(d.get("policy_intent", "")), "night_owl_care", "nonwrap_soft_quiet")
	engine.queue_free()

func _test_sliding_window() -> void:
	# 滑窗累计：45s-5min 停顿按 50% 计入；>5min 清零
	var collector = ContextCollectorScript.new()
	add_child(collector)
	# 活跃 60s → 停 120s（思考）→ 应累计 60 + 120*0.5 = 120
	collector.record_input("typing")
	collector.update_context(60.0)
	collector._last_input_unix = int(Time.get_unix_time_from_system()) - 120  # 模拟停了 2 分钟
	collector.update_context(1.0)  # 触发一次判定
	_assert_true(float(collector.get_snapshot().get("continuous_active_seconds", 0.0)) >= 100.0,
		"pause_counts_half")
	# 停 6 分钟 → 清零
	collector._last_input_unix = int(Time.get_unix_time_from_system()) - 360
	collector.update_context(1.0)
	_assert_true(float(collector.get_snapshot().get("continuous_active_seconds", -1.0)) < 60.0,
		"long_pause_resets")
	collector.queue_free()

func _test_cooldown_multiplier() -> void:
	# 档位冷却倍率：安静 ×4 → 首触发后 300s 内同名动作不重复（基础 1200 的 idle_company 不适用，用 break_hint 900 基准）
	var engine = PolicyEngineScript.new()
	add_child(engine)
	var snap := {"hour": 14, "quiet_hours_start": 23, "quiet_hours_end": 8,
		"fullscreen": false, "continuous_active_seconds": 4000.0,
		"presence_level": 0, "psyche": {"energy": 100.0}}
	var d1: Dictionary = engine.evaluate(snap, [], [], {"personality": "tsundere"})
	_assert_equal(String(d1.get("policy_intent", "")), "long_focus", "first_break_hint_fires")
	# 安静档 ×4：900×4=3600s 内同 intent 不再触发 → 立刻再 evaluate 应 react=false
	engine._last_trigger_time["break_hint"] = int(Time.get_unix_time_from_system()) - 3000
	var d2: Dictionary = engine.evaluate(snap, [], [], {"personality": "tsundere"})
	_assert_true(not bool(d2.get("react", true)), "quiet_multiplier_blocks")
	engine.queue_free()

func _test_intent_whitelist() -> void:
	# 安静档不触发闲聊类；中频档触发饭点
	var engine = PolicyEngineScript.new()
	add_child(engine)
	# 饭点 12:10 活跃：中频触发 meal_hint，安静档不触发
	var snap := {"hour": 12, "minute": 10, "quiet_hours_start": 23, "quiet_hours_end": 8,
		"fullscreen": false, "continuous_active_seconds": 600.0,
		"psyche": {"energy": 100.0}}
	snap["presence_level"] = 0
	var quiet_d: Dictionary = engine.evaluate(snap.duplicate(), [], [], {"personality": "tsundere"})
	_assert_true(String(quiet_d.get("policy_intent", "")) != "meal_hint", "quiet_no_meal_hint")
	snap["presence_level"] = 2
	var mid_d: Dictionary = engine.evaluate(snap.duplicate(), [], [], {"personality": "tsundere"})
	_assert_equal(String(mid_d.get("policy_intent", "")), "meal_hint", "medium_meal_hint")
	engine.queue_free()

func _assert_true(cond: bool, name: String) -> void:
	if cond:
		_passed += 1
	else:
		_failed += 1
		_failures.append(name)

func _assert_equal(actual, expected, name: String) -> void:
	if actual == expected:
		_passed += 1
	else:
		_failed += 1
		_failures.append("%s (expected=%s actual=%s)" % [name, str(expected), str(actual)])

func _print_summary() -> void:
	print("passed: %d  failed: %d" % [_passed, _failed])
	for f in _failures:
		print("  FAIL: " + f)
```

同时创建 `tests/test_presence_level.tscn`（同 Task 1 Step 2 格式，Script 路径换成本文件）。

- [x] **Step 2: 跑测试确认失败**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: FAIL（`presence_level` 属性不存在 / intent 判定不符）

- [x] **Step 3: customization_service.gd 实现**

```gdscript
var presence_level: int = 2  # 0安静 1低频 2中频 3高频 4智能

func apply_settings(settings: Dictionary) -> void:
	# ...原有行保持不变，仅删掉 reminder_intensity 赋值行，加：
	presence_level = clampi(int(settings.get("presence_level", _legacy_reminder_to_presence(settings))), 0, 4)

## 旧 reminder_intensity 迁移映射（presence_level 缺失时兜底）
func _legacy_reminder_to_presence(settings: Dictionary) -> int:
	match String(settings.get("reminder_intensity", "medium")):
		"low":
			return 1
		"high":
			return 3
		_:
			return 2

func get_persona() -> Dictionary:
	return {
		"personality": personality,
		"presence_level": presence_level
	}

func to_settings_dict() -> Dictionary:
	# reminder_intensity 行替换为：
	return {
		# ...其余键保持，加：
		"presence_level": presence_level,
	}
```

- [x] **Step 4: save_manager.gd 四点贯通**

`_get_default_data`：settings 字典里 `"reminder_intensity": "medium"` 行替换为 `"presence_level": 2`。
`load_data`：

```gdscript
	# reminder_intensity 读取行替换为（含迁移：旧档读出旧键时映射）：
	var raw_presence = config.get_value("settings", "presence_level", -1)
	if raw_presence == -1:
		# 旧档迁移：reminder_intensity low/medium/high → 1/2/3
		var legacy: String = String(config.get_value("settings", "reminder_intensity", "medium"))
		settings["presence_level"] = 1 if legacy == "low" else (3 if legacy == "high" else 2)
	else:
		settings["presence_level"] = int(raw_presence)
```

`_write_config`：`config.set_value("settings", "reminder_intensity", ...)` 行替换为 `config.set_value("settings", "presence_level", int(settings.get("presence_level", 2)))`。

- [x] **Step 5: 跑测试**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: 迁移与往返用例 PASS；静音/滑窗/白名单用例仍 FAIL（Task 9-11 实现）——**本步只要求 presence 相关 7 断言通过**，其余失败正常。

- [x] **Step 6: Commit**

```bash
git add customization_service.gd save_manager.gd tests/test_presence_level.gd tests/test_presence_level.tscn
git commit -m "feat: 存在感档位设置键与旧档迁移"
```

---

### Task 8: 设置面板档位 UI

**Files:**
- Modify: `settings_panel.gd`（ReminderIntensityOption → PresenceLevelOption）
- Modify: `settings_panel.tscn`（节点改名与文案）

- [x] **Step 1: settings_panel.tscn 中 ReminderIntensityOption 节点重命名为 PresenceLevelOption**（保持 OptionButton 类型与位置）；检查是否有引用它的 Label 文案（"提醒强度"→"存在感"）

- [x] **Step 2: settings_panel.gd 修改**

```gdscript
# @onready 行：
@onready var presence_level_option: OptionButton = $ScrollContainer/VBoxContainer/PresenceLevelOption

# _ready 中 reminder_intensity_option 的 3 个 add_item 替换为：
	if presence_level_option.get_item_count() == 0:
		presence_level_option.add_item("安静（≤1句/小时）")
		presence_level_option.add_item("低频（关键时刻）")
		presence_level_option.add_item("中频（工位同事）")
		presence_level_option.add_item("高频（话痨猫）")
		presence_level_option.add_item("智能（按活动自适应）")

# apply_settings（84 行附近）reminder_intensity 读取替换：
	presence_level_option.select(clampi(int(settings.get("presence_level", 2)), 0, 4))

# collect_settings 中 match reminder_intensity_option 块整体替换：
	settings["presence_level"] = clampi(presence_level_option.selected, 0, 4)

# _apply_smart_settings 的 configure 字典中 reminder_intensity 行替换：
			"presence_level": clampi(presence_level_option.selected, 0, 4),
```

同时删除 `_reminder_to_index` / `_index_to_reminder` 两个函数（无引用后）。信号连接：若 tscn 中 ReminderIntensityOption 有 item_selected 信号连到 `_on_reminder_selected`，改为 presence 的 item_selected 直连 `_apply_smart_settings`（或新增空处理 `_on_presence_selected(_i)` 调 `_apply_smart_settings()`）。

- [x] **Step 3: 回归**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn && timeout 90 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn
```
Expected: 双绿（smart_modules 里若有用 reminder_intensity 的用例，更新为 presence_level 等价断言——执行时看失败信息定）

- [x] **Step 4: Commit**

```bash
git add settings_panel.gd settings_panel.tscn
git commit -m "feat: 设置面板存在感档位选择器"
```

---

### Task 9: context_collector 滑动窗口累计（死链②）

**Files:**
- Modify: `context_collector.gd:30-37`（update_context）
- Test: `tests/test_presence_level.gd`（已含 _test_sliding_window）

- [x] **Step 1: 确认测试当前失败**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: `pause_counts_half` / `long_pause_resets` FAIL

- [x] **Step 2: 实现 update_context 滑窗口径**

```gdscript
const PAUSE_FULL_SEC := 300.0   # 停止输入超 5 分钟视为连续活跃中断
const PAUSE_DISCOUNT := 0.5     # 45s~5min 的思考间隙按 50% 折算

func update_context(delta: float) -> void:
	var now := int(Time.get_unix_time_from_system())
	_idle_seconds = float(now - _last_input_unix)
	if _idle_seconds <= 45.0:
		_active_seconds += delta
		_continuous_active_seconds += delta
	elif _idle_seconds <= PAUSE_FULL_SEC:
		# 思考间隙：半速折算，不打断连续活跃
		_continuous_active_seconds += delta * PAUSE_DISCOUNT
	else:
		_continuous_active_seconds = 0.0
```

- [x] **Step 3: 跑测试通过**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: 两个滑窗断言 PASS

- [x] **Step 4: 回归 test_smart_modules（确保无既有断言依赖 45s 清零语义）**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn`
Expected: 全绿（若有依赖旧语义的断言，按新口径更新断言值，不算行为回归）

- [x] **Step 5: Commit**

```bash
git add context_collector.gd tests/test_presence_level.gd
git commit -m "fix: 连续活跃改滑动窗口累计修复长时意图不可达"
```

---

### Task 10: policy_engine 分层静音（死链①）

**Files:**
- Modify: `reaction_policy_engine.gd`
- Test: `tests/test_presence_level.gd`（已含 _test_soft_hard_quiet）

- [x] **Step 1: 实现分层静音**

reaction_policy_engine.gd 中 `_is_quiet_hours` 拆为两个函数，evaluate 入口的拦截改为硬静音才返回 quiet_mode、软静音放行轻关怀：

```gdscript
const SOFT_QUIET_MINUTES := 60  # 静音时段前 60 分钟为软静音（进入缓冲）

## 硬静音：完全静默（quiet_mode 仅此处返回）
func _is_hard_quiet(snapshot: Dictionary) -> bool:
	if not _in_quiet_window(snapshot):
		return false
	return not _in_soft_quiet(snapshot)

## 软静音：静音时段前 60 分钟，允许轻关怀类意图
func _in_soft_quiet(snapshot: Dictionary) -> bool:
	if not _in_quiet_window(snapshot):
		return false
	var minute_of_day: int = int(snapshot.get("hour", 12)) * 60 + int(snapshot.get("minute", 0))
	var start_minute: int = int(snapshot.get("quiet_hours_start", 23)) * 60
	# start→start+60 为软静音（跨午夜场景 start 在深夜，分钟值直接可比较）
	return minute_of_day >= start_minute and minute_of_day < start_minute + SOFT_QUIET_MINUTES

func _in_quiet_window(snapshot: Dictionary) -> bool:
	var hour: int = int(snapshot.get("hour", 12))
	var start_hour: int = int(snapshot.get("quiet_hours_start", 23))
	var end_hour: int = int(snapshot.get("quiet_hours_end", 8))
	if start_hour == end_hour:
		return false
	if start_hour < end_hour:
		return hour >= start_hour and hour < end_hour
	return hour >= start_hour or hour < end_hour
```

evaluate() 开头（36 行附近）替换：

```gdscript
	if _is_hard_quiet(snapshot) or bool(snapshot.get("fullscreen", false)):
		return _decide(now, ACTION_SLEEP_CURL, "quiet_mode", _line_for("quiet_mode", personality), energy)
```

深夜关怀判定（42 行附近）条件改为软静音窗口判定替代小时写死：

```gdscript
	# 深夜关怀：软静音时段内（原 23-1 点被静音全灭）+ 滑窗活跃 30 分钟
	if _in_soft_quiet(snapshot) and continuous_active_precheck(snapshot, 1800.0):
		var care_line := "这么晚还在忙，记得早点休息。"
		if memory_lines.size() > 0:
			care_line = String(memory_lines[0])
		return _decide(now, ACTION_COMFORT, "night_owl_care", care_line, energy)
```

原 `_is_quiet_hours` 保留为 `_in_quiet_window` 改名（smart_pet_controller.gd:170 也调用了它，同步改调用点）。

- [x] **Step 2: 跑测试**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: soft_quiet_allows_care / hard_quiet_silences / nonwrap_soft_quiet PASS

- [x] **Step 3: 回归 test_smart_modules（night_owl_care 旧用例若按 hour>=23 判定，需同步为新语义）**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn`
Expected: 全绿；旧 `_test_night_owl_care` 用例改为传 `"minute": 30` 等符合软静音窗口的快照

- [x] **Step 4: Commit**

```bash
git add reaction_policy_engine.gd smart_pet_controller.gd tests/test_smart_modules.gd tests/test_presence_level.gd
git commit -m "fix: 分层静音放行深夜关怀修复旗舰意图死链"
```

---

### Task 11: 意图白名单 + 冷却倍率 + 新意图 + 删除死代码（死链③）

**Files:**
- Modify: `reaction_policy_engine.gd`
- Modify: `main.gd`（删 _build_failure_streak / report_build_result / BUILD_FAILURE_STREAK_THRESHOLD）
- Test: `tests/test_presence_level.gd`（已含 cooldown/whitelist 用例）

- [x] **Step 1: policy_engine 加档位参数与白名单**

```gdscript
## 档位 → 冷却倍率（意图冷却 = 基础值 × 倍率）
const PRESENCE_COOLDOWN_MULT := {0: 4.0, 1: 2.0, 2: 1.0, 3: 0.5, 4: 1.0}
## 档位 → 解锁意图（低档是高档子集）
const INTENT_MIN_PRESENCE := {
	"welcome_back": 0, "long_focus": 0, "focus_milestone": 0, "focus_failed": 0,
	"night_owl_care": 1, "meal_hint": 1, "slacking_caught": 1,
	"chitchat": 2, "weather_smalltalk": 2,
	"invite_play": 3, "throw_yarn": 3,
	"video_companion": 1, "coding_cheer": 1,
	"quiet_mode": 0, "user_busy": 1,
}

func _intent_allowed(intent: String, presence: int) -> bool:
	return int(INTENT_MIN_PRESENCE.get(intent, 2)) <= presence

func _cooldown_for(action_id: String, presence: int) -> int:
	return int(float(int(DEFAULT_COOLDOWN.get(action_id, 120))) * float(PRESENCE_COOLDOWN_MULT.get(presence, 1.0)))
```

`_decision_if_available` 签名加 presence 参数并用 `_cooldown_for`；各 `_decide` 调用链把 `presence_level`（从 snapshot 读，默认 2）传下去。evaluate() 里每个意图 return 前加白名单闸：

```gdscript
	var presence: int = clampi(int(snapshot.get("presence_level", 2)), 0, 4)
	# 例：久坐提醒
	if continuous_active >= active_threshold and _intent_allowed("long_focus", presence):
		return _decide(now, ACTION_BREAK_HINT, "long_focus", ...)
```

注意 active_threshold 的 reminder_intensity 分支删除，改为档位微调：安静档阈值 +1200s、高频 -600s（保持"安静更晚提醒"语义）。

- [x] **Step 2: 新增 3 个意图（饭点/摸鱼抓包/天气闲聊）**

在 evaluate() 的 coding_cheer 之后追加：

```gdscript
	# —— P5b 短平快意图 ——
	if _intent_allowed("meal_hint", presence) and _is_meal_time(snapshot) \
			and continuous_active_precheck(snapshot, 300.0):
		return _decide(now, ACTION_TAIL_WAG, "meal_hint", _line_for("meal_hint", personality), energy)
	if _intent_allowed("slacking_caught", presence) and String(snapshot.get("activity", "")) in ["browsing", "video"] \
			and float(snapshot.get("activity_seconds", 0.0)) >= 600.0:
		return _decide(now, ACTION_TAIL_WAG, "slacking_caught", _line_for("slacking_caught", personality), energy)
	if _intent_allowed("weather_smalltalk", presence) and _is_first_decision_of_hour(snapshot):
		return _decide(now, ACTION_IDLE_COMPANY, "weather_smalltalk", _line_for("weather_smalltalk", personality), energy)

func _is_meal_time(snapshot: Dictionary) -> bool:
	var hour: int = int(snapshot.get("hour", 12))
	var minute: int = int(snapshot.get("minute", 0))
	var mod := hour * 60 + minute
	return (mod >= 720 and mod <= 780) or (mod >= 1080 and mod <= 1140)  # 12-13 / 18-19 点

func _is_first_decision_of_hour(snapshot: Dictionary) -> bool:
	# 每小时第一次决策：minute < 3（policy_tick 3s，档位下第一轮必中）
	return int(snapshot.get("minute", 0)) < 3
```

`_line_for` 追加三个意图的台词（三性格各一句，格式照抄现有 match 分支）：

```gdscript
		"meal_hint":
			if personality == "playful":
				return "饭点到啦！走，干饭去！"
			if personality == "gentle":
				return "到饭点啦，先去吃饭吧。"
			return "都饭点了，别卷了，去吃饭。"
		"slacking_caught":
			if personality == "playful":
				return "抓到你摸鱼啦！嘿嘿！"
			if personality == "gentle":
				return "休息一下也好，眼睛看看远处吧。"
			return "哦？这就开始摸鱼了？"
		"weather_smalltalk":
			if personality == "playful":
				return "今天过得怎么样呀？"
			if personality == "gentle":
				return "忙里偷闲，和你说说话。"
			return "喂，偶尔也看看我嘛。"
```

新意图动作映射（main.gd `_map_smart_action_to_animation` 无需改，TAIL_WAG/IDLE_COMPANY 复用现有映射）。

- [x] **Step 3: 智能档自适应与扔东西意图**

智能档（presence=4）：evaluate() 读 snapshot activity，工作类（coding/writing）按安静档冷却倍率、娱乐类（video/game/browsing）按高频——实现为档位换算函数：

```gdscript
func _effective_presence(snapshot: Dictionary, presence: int) -> int:
	## 智能档按当前活动折算等效档位；其余档位原样返回
	if presence != 4:
		return presence
	match String(snapshot.get("activity", "")):
		"coding", "writing":
			return 0  # 工作时压到安静
		"video", "game", "browsing":
			return 3  # 休闲时升到高频
		_:
			return 2
```

evaluate() 里 `var presence := ...` 之后加 `presence = _effective_presence(snapshot, presence)`（白名单与冷却全部用换算后的值；LLM 周期表 PRESENCE_LLM_TICK[4]=45s 不动，提示词人设仍按原始档 4 注入）。测试：activity=coding + presence=4 → meal_hint 被白名单拦（等效 0）；activity=video + presence=4 → 触发。

扔东西（throw_yarn，高频专属）：chaos ≥ 60 且 energy ≥ 40 时 10% 概率（同样用 `_invite_chance` 式 override 可注入），动作复用 pounce、意图台词「毛线球好像在看我……下去吧！」。触发后 main 侧 spawn 一个 yarn 道具在猫爪位置并给它初速度（`_map_smart_action_to_animation` 加 `throw_yarn → pounce` 映射 + smart_action_requested 的 decision 带 `spawn_item: "yarn"` 标记，main 判定后 spawn）。测试：chaos=70 + presence=3 + override → intent=throw_yarn；presence=2 → 不触发。

- [x] **Step 4: 删除 build_fail_streak 死代码**

- reaction_policy_engine.gd：删 `if _has_recent_event(recent_events, "build_fail_streak", 240):` 整块（54-55 行）与 `_line_for` 的 `"build_fail_streak"` 分支
- main.gd：删 `BUILD_FAILURE_STREAK_THRESHOLD` 常量、`_build_failure_streak` 变量声明与函数、`report_build_result()` 函数、`_on_focus_session_finished` 里的 `_record_build_failure_streak` 调用行
- test_smart_modules.gd：删/改 `_test_policy_build_fail_streak` 用例（改为断言 evaluate 对含 build_fail_streak 事件的快照返回 react=false）

- [x] **Step 5: 跑全部相关测试**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn && \
timeout 90 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn && \
timeout 90 "$GODOT" --headless --path . res://tests/test_behavior_system.tscn
```
Expected: 三绿

- [x] **Step 6: Commit**

```bash
git add reaction_policy_engine.gd main.gd tests/test_presence_level.gd tests/test_smart_modules.gd
git commit -m "feat: 意图白名单与冷却倍率并删除构建连败死代码"
```

---

### Task 12: llm_adapter 档位人设 + 沉默反压 + 润色轻请求

**Files:**
- Modify: `llm_adapter.gd`
- Test: `tests/test_presence_level.gd`（追加）

- [x] **Step 1: 追加失败测试**

```gdscript
const LLMAdapterScript = preload("res://llm_adapter.gd")

func _test_llm_presence_persona() -> void:
	var adapter = LLMAdapterScript.new()
	add_child(adapter)
	# 档位人设注入：prompt 含档位描述与 silent_streak
	var payload := {
		"persona": {"personality": "tsundere", "presence_level": 3},
		"psyche": {}, "context": {"hour": 14},
		"memory_lines": [], "recent_events": [], "tags": [],
		"silent_streak": 4}
	var prompt: String = adapter._build_decision_prompt(payload)
	_assert_true(prompt.contains("chatty"), "persona_has_chatty")
	_assert_true(prompt.contains("silent for the last 4 cycles"), "silent_streak_injected")

	var persona2 := {"personality": "tsundere", "presence_level": 0}
	var payload2 := payload.duplicate(true)
	payload2["persona"] = persona2
	payload2["silent_streak"] = 0
	var prompt2: String = adapter._build_decision_prompt(payload2)
	_assert_true(prompt2.contains("quiet"), "persona_has_quiet")
	_assert_true(not prompt2.contains("silent for the last"), "no_streak_when_zero")

	# 润色轻请求：独立 system prompt
	var polish: String = adapter._build_polish_prompt({"persona": persona2}, "模板台词")
	_assert_true(polish.contains("模板台词"), "polish_contains_template")
	adapter.queue_free()
```

_run() 中追加 `_test_llm_presence_persona()` 调用。

- [x] **Step 2: 跑测试确认失败后实现**

llm_adapter.gd：

```gdscript
## 档位 → system prompt 人设段
const PRESENCE_PERSONA := {
	0: "You are a very quiet cat. Speak only for truly important moments (long sitting, victories). At most 1 line per hour.",
	1: "You speak at key moments: meal time, late night, long sitting. 1-3 lines per hour.",
	2: "You are a friendly co-worker cat. Naturally chime in every 10-15 minutes. Gentle teasing when the user slacks off is fine.",
	3: "You are a chatty cat. Almost every decision should show something (action or line). Actively invite interaction.",
	4: "You adapt to the user's current activity: restrained when they work, playful when they relax. The activity category is provided below.",
}

func _build_decision_prompt(payload: Dictionary) -> String:
	# ...原有字段拼接保持，改动两处：
	# 1) personality= 行后追加档位人设：
	#    presence_persona=%s
	# 2) silent_streak 注入：
	var persona_raw = payload.get("persona", {})
	var persona: Dictionary = persona_raw if typeof(persona_raw) == TYPE_DICTIONARY else {}
	var presence := clampi(int(persona.get("presence_level", 2)), 0, 4)
	var streak := int(payload.get("silent_streak", 0))
	var streak_note := ""
	if streak >= 2:
		streak_note = "You have stayed silent for the last %d cycles. If there is anything at all worth reacting to, speak now." % streak
	# 模板 %s 数组对应位置插入 PRESENCE_PERSONA.get(presence, PRESENCE_PERSONA[2]) 与 streak_note

## 润色轻请求：模板台词 → 猫味台词（独立小 prompt，超时 5s 放弃）
func generate_polish_async(payload: Dictionary, template_line: String) -> void:
	_decision_mode = false
	_fallback_line = template_line
	_fallback_action = ""
	_send_request(SYSTEM_PROMPT_LINE, _build_polish_prompt(payload, template_line))

func _build_polish_prompt(payload: Dictionary, template_line: String) -> String:
	var persona_raw = payload.get("persona", {})
	var persona: Dictionary = persona_raw if typeof(persona_raw) == TYPE_DICTIONARY else {}
	return "Rewrite this line as a cat with personality=%s (tsundere=傲娇, gentle=温柔, playful=活泼). Keep it under 25 chars, Simplified Chinese, keep the meaning. Line: %s" % [
		String(persona.get("personality", "tsundere")), template_line]
```

SYSTEM_PROMPT_LINE 现有文案可保留（润色与旧 line 模式共用入口）。注意 `configure` 时润色请求 timeout 需要单独短超时——加 `var _polish_timeout := 5.0`，`generate_polish_async` 里 `_http_request.timeout = _polish_timeout`，请求完成回调后恢复 `timeout_seconds`。

- [x] **Step 3: 跑测试 + 回归 smart_modules**

Run: `timeout 60 "$GODOT" --headless --path . res://tests/test_presence_level.tscn`
Expected: 全绿

- [x] **Step 4: Commit**

```bash
git add llm_adapter.gd tests/test_presence_level.gd
git commit -m "feat: LLM 档位人设与沉默反压润色轻请求"
```

---

### Task 13: smart_pet_controller 双轨编排

**Files:**
- Modify: `smart_pet_controller.gd`
- Test: `tests/test_presence_level.gd`（追加）

- [x] **Step 1: 实现双轨**

smart_pet_controller.gd 核心改动：

```gdscript
## 档位 → LLM 自主决策周期（秒）
const PRESENCE_LLM_TICK := {0: 180.0, 1: 90.0, 2: 45.0, 3: 20.0, 4: 45.0}
var _silent_streak: int = 0
var _polish_deadline_ms: int = 0

func _process(delta: float) -> void:
	if not _customization_service.smart_mode:
		return
	_context_collector.update_context(delta)
	# 轨道A：规则引擎按固定节奏独立判定（不再因 LLM 开启而停摆）
	_timer += delta
	var policy_interval := policy_tick_interval
	if _timer < policy_interval:
		return
	_timer = 0.0
	var decision := _build_decision()  # 抽出原 _evaluate_policy 的快照/tags/decision 组装
	if bool(decision.get("react", false)):
		_rule_track_fire(decision)

	# 轨道B：LLM 自主（按档位周期）
	_llm_timer += delta
	var llm_interval: float = PRESENCE_LLM_TICK.get(_customization_service.presence_level, 45.0)
	if _customization_service.llm_enabled and _llm_timer >= llm_interval:
		_llm_timer = 0.0
		_llm_track_ask(decision)

func _rule_track_fire(decision: Dictionary) -> void:
	# 保底心跳：动作+模板台词立即上，不等 LLM
	record_event("smart_decision", {"action_id": decision.action_id, "intent": decision.policy_intent})
	smart_action_requested.emit(decision.action_id, decision)
	smart_line_generated.emit(decision.template_line, "policy")
	# 异步润色（LLM 开启时）：5s 内回来且气泡仍在显示则替换
	if _customization_service.llm_enabled:
		_polish_deadline_ms = Time.get_ticks_msec() + 5000
		_llm_adapter.generate_polish_async(
			{"persona": _customization_service.get_persona()}, String(decision.template_line))

func _llm_track_ask(decision: Dictionary) -> void:
	# 硬安全闸保留（硬静音/全屏不问）；软静音放行
	if _policy_engine._is_hard_quiet(_last_snapshot) or bool(_last_snapshot.get("fullscreen", false)):
		return
	# ...原 _evaluate_policy 的 LLM 分支逻辑，payload 增加 silent_streak
```

`_on_llm_line_ready` 判断润色时效：

```gdscript
func _on_llm_line_ready(line: String, source: String, _meta: Dictionary) -> void:
	if source == "llm" and Time.get_ticks_msec() > _polish_deadline_ms and _polish_deadline_ms > 0:
		return  # 润色超时，气泡可能已淡出，丢弃
	smart_line_generated.emit(line, source)
```

`_on_llm_no_reaction` 增加 `_silent_streak += 1`；`_on_llm_decision_ready` 重置 `_silent_streak = 0`。LLM payload `"silent_streak": _silent_streak` 注入。注意原 `_evaluate_policy()` 单函数拆成 `_build_decision()`（组装快照/tags/persona/decision 并缓存 `_last_snapshot`）+ 两个轨道函数，`_last_snapshot` 为新成员变量。原 LLM 分支里 `interval = llm_tick_interval` 逻辑删除（双轨接管）。

- [x] **Step 2: 追加测试（轨道分离最小验证）**

```gdscript
func _test_dual_track_independent() -> void:
	# 规则引擎不因 llm_enabled 停摆：controller 在 llm 开启时规则意图仍 emit
	var ctrl = preload("res://smart_pet_controller.gd").new()
	add_child(ctrl)
	ctrl.bind_behavior(null)
	ctrl.configure({"smart_mode": true, "llm_enabled": true, "presence_level": 2,
		"quiet_hours_start": 23, "quiet_hours_end": 8})
	var fired: Array[String] = []
	ctrl.smart_line_generated.connect(func(line, source): fired.append(source))
	# 注入深夜软静音+长活跃快照，快进 policy tick
	ctrl._context_collector._continuous_active_seconds = 2000.0
	ctrl._last_snapshot = {"hour": 23, "minute": 30, "quiet_hours_start": 23, "quiet_hours_end": 8}
	for i in range(60):
		ctrl._process(0.06)  # 3.6s > policy_tick_interval
	_assert_true(fired.has("policy"), "rule_track_fires_with_llm_on")
	ctrl.queue_free()
```

注意：`_process` 内 LLM 轨道会真的发起 HTTP——测试里 `_customization_service.llm_enabled` 传 true 但 api_key 留空，`_send_request` 走 `api_key_missing` 直接 fallback（emit "policy" source 的 fallback line，同断言不冲突）。若 `_build_decision` 需要 `_last_snapshot` 之外的输入导致该测试不稳定，允许把断言放宽为 `fired.size() > 0`。

- [x] **Step 3: 跑测试 + 回归**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn && \
timeout 90 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn
```
Expected: 双绿

- [x] **Step 4: Commit**

```bash
git add smart_pet_controller.gd tests/test_presence_level.gd
git commit -m "feat: 双轨节奏规则保底与LLM润色自主"
```

---

### Task 14: P5b 收尾 — 全量回归 + MCP 端到端

- [x] **Step 1: 九+1 套测试全量**（命令同 Task 3，加 test_presence_level），记录断言总数对比 P5a 基线只增不减
- [x] **Step 2: MCP 端到端**：
  - 启动项目，设置面板切档位到"高频"
  - 观察 15 分钟（或注入快进）：≥3 次猫行为输出（气泡/动作），日志 `[SMART/...]` 出现
  - 静音时段验证（改系统时间或临时把 quiet_hours_start 设为当前小时）：软静音内深夜关怀触发、硬静音内无任何气泡
  - LLM 关闭验证：规则引擎独立出话（`[SMART/policy]` 日志）
- [x] **Step 3: 收尾提交**（若有收尾修复）+ 把验证证据写进本文件

---

# P5c 道具激活闭环

### Task 15: 首次引导状态机

**Files:**
- Create: `components/engagement/first_guide_controller.gd`
- Modify: `main.gd`（挂载 + 接线）
- Modify: `save_manager.gd`（behavior 区 first_interaction_done 键——走 behavior_system 存档通道或 settings 四点，选后者更简单）
- Create: `tests/test_first_guide.gd` + `.tscn`

- [x] **Step 1: 写失败测试**

```gdscript
extends Node

const GuideScript = preload("res://components/engagement/first_guide_controller.gd")

var _passed: int = 0
var _failed: int = 0
var _failures: Array[String] = []

func _ready() -> void:
	call_deferred("_run")

func _run() -> void:
	_test_guide_flow()
	_print_summary()
	get_tree().quit(0 if _failed == 0 else 1)

func _test_guide_flow() -> void:
	# 状态机：未引导→step1(摸摸)→step2(道具)→done；done 不再触发
	var guide = GuideScript.new()
	add_child(guide)
	_assert_equal(guide.stage, 0, "starts_at_zero")
	guide.start()
	_assert_equal(guide.stage, 1, "start_enters_step1")
	guide.notify_interaction("pet")
	_assert_equal(guide.stage, 2, "pet_completes_step1")
	guide.notify_interaction("food")
	_assert_equal(guide.stage, 3, "item_completes_step2")
	# 完成后重置实例不回退
	guide.notify_interaction("pet")
	_assert_equal(guide.stage, 3, "done_is_terminal")
	# 已完成的实例 start() 是 no-op
	guide.start()
	_assert_equal(guide.stage, 3, "restart_noop_after_done")
	# step2 超时跳过（10 分钟无互动自动完成）
	var guide2 = GuideScript.new()
	add_child(guide2)
	guide2.start()
	guide2.notify_interaction("pet")
	guide2._step2_deadline_unix = int(Time.get_unix_time_from_system()) - 1
	guide2.tick()
	_assert_equal(guide2.stage, 3, "step2_timeout_completes")
	guide.queue_free()
	guide2.queue_free()

func _assert_equal(actual, expected, name: String) -> void:
	if actual == expected:
		_passed += 1
	else:
		_failed += 1
		_failures.append("%s (expected=%s actual=%s)" % [name, str(expected), str(actual)])

func _assert_true(cond: bool, name: String) -> void:
	if cond:
		_passed += 1
	else:
		_failed += 1
		_failures.append(name)

func _print_summary() -> void:
	print("passed: %d  failed: %d" % [_passed, _failed])
	for f in _failures:
		print("  FAIL: " + f)
```

- [x] **Step 2: 实现 components/engagement/first_guide_controller.gd**

```gdscript
class_name FirstGuideController
extends Node

## 首次引导状态机（非弹窗）：stage 0=未引导 1=引导摸摸 2=引导道具 3=完成
## 由 main 挂载；stage1 启动 30 秒后猫走到屏幕中下说话；互动事件经 notify_interaction 喂入

signal guide_stage_changed(stage: int, message: String)

const STEP1_DELAY_SEC := 30.0
const STEP2_TIMEOUT_SEC := 600.0

var stage: int = 0
var _step2_deadline_unix: int = 0
var _elapsed: float = 0.0
var _step1_fired: bool = false

func load_from_save(done: bool) -> void:
	stage = 3 if done else 0

func is_done() -> bool:
	return stage >= 3

func start() -> void:
	if is_done():
		return
	if stage == 0:
		stage = 1
		_elapsed = 0.0
		_step1_fired = false
		guide_stage_changed.emit(stage, "")

func tick(delta: float = 0.0) -> void:
	if stage == 1 and not _step1_fired:
		_elapsed += delta
		if _elapsed >= STEP1_DELAY_SEC:
			_step1_fired = true
			guide_stage_changed.emit(1, "摸摸我吧")
	elif stage == 2:
		if _step2_deadline_unix > 0 and Time.get_unix_time_from_system() > _step2_deadline_unix:
			stage = 3
			guide_stage_changed.emit(3, "")

func notify_interaction(kind: String) -> void:
	if stage == 1 and kind in ["pet", "food", "wand"]:
		stage = 2
		_step2_deadline_unix = int(Time.get_unix_time_from_system() + STEP2_TIMEOUT_SEC)
		guide_stage_changed.emit(2, "想玩的话，点我有惊喜")
	elif stage == 2 and kind in ["food", "wand", "yarn", "box"]:
		stage = 3
		guide_stage_changed.emit(3, "")
```

- [x] **Step 3: 跑测试通过，main.gd 接线**

main.gd：`_ready` 中创建 FirstGuideController、add_child；读档后 `guide.load_from_save(bool(settings.get("first_interaction_done", false)))`；未完成则 `guide.start()`。`guide_stage_changed` 连接：stage1/2 消息走 smart_line_bubble 气泡（message 非空时）；互动埋点：`_on_quick_action_selected` 里 `guide.notify_interaction(action)`（pet/food/wand/yarn/box 均覆盖）；撸猫结束（cat 的 petting_ended 链路，main 里若监听则加 `guide.notify_interaction("pet")`——撸猫信号在 cat 内部，main 通过 `_record_smart_event` 侧的 state_changed 兜底或给 cat 加一行转发，选**给 main 已有的 petting 接线点**加，若 main 未监听 petting_ended 则在 cat.gd 的 `_on_petting_ended` 里发 `interaction_performed("pet")` 信号、main 转发，避免 cat 直接依赖 main）。

save_manager 四点加 `first_interaction_done`（bool，默认 false；guide stage==3 时在 `_gather_current_data` 写 true——由 main 持有 guide 引用，SaveManager 走 main provider 链取，与 cat_type 同模式）。

- [x] **Step 4: 回归 + Commit**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_first_guide.tscn && \
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn
git add components/engagement/first_guide_controller.gd main.gd cat.gd save_manager.gd tests/test_first_guide.gd tests/test_first_guide.tscn
git commit -m "feat: 首次引导状态机猫主动教学"
```

---

### Task 16: 主动邀请意图

**Files:**
- Modify: `reaction_policy_engine.gd`（invite_play 意图实体化——Task 11 白名单已含键）
- Modify: `smart_pet_controller.gd`（喂 last_interaction_unix）
- Modify: `context_collector.gd`（快照加 minutes_since_interaction 字段）
- Test: `tests/test_presence_level.gd`（追加）

- [x] **Step 1: 追加失败测试**

```gdscript
func _test_invite_play() -> void:
	var engine = PolicyEngineScript.new()
	add_child(engine)
	# 中频+ 30 分钟无互动 → invite_play（次序在最后，需避开其他意图：hour=14 非饭点非久坐）
	var snap := {"hour": 14, "minute": 5, "quiet_hours_start": 23, "quiet_hours_end": 8,
		"fullscreen": false, "continuous_active_seconds": 300.0,
		"presence_level": 2, "minutes_since_interaction": 45.0,
		"psyche": {"energy": 100.0}}
	var d: Dictionary = engine.evaluate(snap, [], [], {"personality": "tsundere"})
	_assert_equal(String(d.get("policy_intent", "")), "invite_play", "invite_after_idle")
	# 安静档不邀请
	snap["presence_level"] = 0
	var d2: Dictionary = engine.evaluate(snap.duplicate(), [], [], {"personality": "tsundere"})
	_assert_true(String(d2.get("policy_intent", "")) != "invite_play", "quiet_no_invite")
	engine.queue_free()
```

- [x] **Step 2: 实现**

context_collector.gd：`var _last_interaction_unix := 0` + `record_interaction()`（item_use/petting/点击猫处调用；main 的 `_record_smart_event` 侧与 cat 的 interaction 信号处各加一行）+ 快照字段：

```gdscript
		"minutes_since_interaction": (float(now - _last_interaction_unix) / 60.0) if _last_interaction_unix > 0 else 9999.0,
```

reaction_policy_engine.gd（user_busy 之前插入）：

```gdscript
	# —— P5c 主动邀请：长时间无互动，猫主动邀请撸猫/玩道具 ——
	var since_interaction: float = float(snapshot.get("minutes_since_interaction", 9999.0))
	if _intent_allowed("invite_play", presence) and since_interaction >= 30.0 \
			and energy >= 30.0 and not _in_quiet_window(snapshot):
		if randf() < _invite_chance(presence):
			return _decide(now, ACTION_GREET, "invite_play", _line_for("invite_play", personality), energy)

func _invite_chance(presence: int) -> float:
	# 每决策周期触发概率：中频 8% 高频 15% 智能按活动（工作 4% 休闲 12%）
	match presence:
		2:
			return 0.08
		3:
			return 0.15
		4:
			return 0.12
		_:
			return 0.08
```

（randf 使测试不稳定 → 测试断言改为概率 1 注入：加 `_invite_chance_override: float = -1.0`，`_invite_chance` 优先返回 override；测试设 `engine._invite_chance_override = 1.0`。）`_line_for` 加 invite_play 三性格台词（"无聊了……那个毛线球，不玩吗？"/"想和我玩一会儿吗？"/"喂，都多久没理我了！"）。冷却：invite_play 加入 DEFAULT_COOLDOWN（`ACTION_GREET` 已有 180——邀请与欢迎共用 greet 动作冷却会互相顶，新增 `ACTION_INVITE := "greet"` 不行；改为 invite_play 用独立冷却键 `"invite_play_greet": 600`，`_decision_if_available` 已按 action_id 记冷却，这里给 `_decide` 传 intent 作为冷却键的变体：直接在 DEFAULT_COOLDOWN 加条目并让 invite_play `_decide` 后手动 `_last_trigger_time["invite_play"] = now`，简单起见 evaluate 内联处理冷却判断）。

简化决定（避免过度工程）：invite_play 冷却直接复用 `DEFAULT_COOLDOWN[ACTION_GREET]`（180s × 档位倍率），与 welcome_back 共用顶掉的风险可接受（同为"猫打招呼"语义）。

- [x] **Step 3: 跑测试 + 回归**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn && \
timeout 90 "$GODOT" --headless --path . res://tests/test_smart_modules.tscn
```

- [x] **Step 4: Commit**

```bash
git add reaction_policy_engine.gd context_collector.gd smart_pet_controller.gd main.gd cat.gd tests/test_presence_level.gd
git commit -m "feat: 主动邀请意图猫主动求互动"
```

---

### Task 17: 主动邀请克制机制（忽略冷却翻倍）

**Files:**
- Modify: `reaction_policy_engine.gd`
- Test: `tests/test_presence_level.gd`（追加）

- [x] **Step 1: 追加失败测试**

```gdscript
func _test_invite_restraint() -> void:
	var engine = PolicyEngineScript.new()
	add_child(engine)
	engine._invite_chance_override = 1.0
	var snap := {"hour": 14, "minute": 5, "quiet_hours_start": 23, "quiet_hours_end": 8,
		"fullscreen": false, "continuous_active_seconds": 300.0,
		"presence_level": 2, "minutes_since_interaction": 45.0,
		"psyche": {"energy": 100.0}}
	# 首次邀请触发
	var d: Dictionary = engine.evaluate(snap.duplicate(), [], [], {"personality": "tsundere"})
	_assert_equal(String(d.get("policy_intent", "")), "invite_play", "first_invite")
	# 冷却内再触发被拦（180s×1.0 倍率）
	snap["minutes_since_interaction"] = 46.0
	engine._last_trigger_time["greet"] = int(Time.get_unix_time_from_system()) - 100
	d = engine.evaluate(snap.duplicate(), [], [], {"personality": "tsundere"})
	_assert_true(String(d.get("policy_intent", "")) != "invite_play", "cooldown_blocks")
	# 忽略翻倍：register_invite_ignored 一次 → 基础冷却变 360
	engine.register_invite_ignored()
	_assert_true(engine._invite_cooldown_sec() >= 360, "ignored_doubles_cooldown")
	engine.queue_free()
```

- [x] **Step 2: 实现**

```gdscript
var _invite_ignore_count: int = 0

func register_invite_ignored() -> void:
	## 邀请后 10 分钟内无互动 → main 调用（判定放 main：邀请时间戳与互动时间戳对比）
	_invite_ignore_count += 1
	if _invite_ignore_count >= 3:
		_invite_ignore_today = true  # 当日不再邀请

var _invite_ignore_today := false
var _invite_ignore_day := -1

func _invite_cooldown_sec() -> int:
	var base := int(DEFAULT_COOLDOWN[ACTION_GREET])
	return base * (1 << mini(_invite_ignore_count, 3))  # 180→360→720→1440

func _invite_blocked_today() -> bool:
	var today := int(Time.get_date_dict_from_system().day)
	if today != _invite_ignore_day:
		_Invite_ignore_day_reset()
	return _invite_ignore_today

func _Invite_ignore_day_reset() -> void:
	_invite_ignore_day = int(Time.get_date_dict_from_system().day)
	_invite_ignore_today = false
	_invite_ignore_count = 0
```

（注意 `_Invite_ignore_day_reset` 命名笔误风险——实现时统一小写 `_invite_day_reset`。）invite_play 判定加 `and not _invite_blocked_today()`，冷却用 `_invite_cooldown_sec()` 替代共享 180（放弃 Task 16 的简化决定，独立冷却，`_last_trigger_time["invite_play"]` 记录）。

main.gd：邀请触发时记 `_last_invite_unix`；`record_interaction` 事件到达（撸猫/道具/点击）时若 `now - _last_invite_unix < 600` 则不忽略；每分钟检查：`_last_invite_unix > 0` 且 600 秒内无互动 → `policy_engine.register_invite_ignored()`，清 `_last_invite_unix`。（此逻辑放 main 的 `_process` 聚合计时器，或简单做法：policy_engine 加 `check_invite_ignored(now_interaction_unix)`——实现时选 main 侧方案，注意加测试或至少人工验证。）

- [x] **Step 3: 跑测试 + 回归 + Commit**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_presence_level.tscn
git add reaction_policy_engine.gd main.gd tests/test_presence_level.gd
git commit -m "feat: 主动邀请忽略冷却翻倍克制机制"
```

---

### Task 18: bond 反馈钩子（气泡小提示 + 进度条 + 饿猫加成）

**Files:**
- Modify: `cat.gd`（撸猫/互动 add_bond 后发 bond_gained 信号）
- Modify: `components/ui/smart_line_bubble.gd`（轻量小提示方法）
- Modify: `main.gd`（接线 + 喂食加成判定）
- Modify: `settings_panel.gd` + `.tscn`（亲密度进度条区）
- Test: `tests/test_bond_system.gd`（追加）

- [x] **Step 1: 追加失败测试**

```gdscript
func _test_hungry_feed_bonus() -> void:
	# 饿猫喂食 ×1.5：bond_system.add_bond 支持 multiplier 参数
	var bond = BondSystemScript.new()
	add_child(bond)
	bond.bond = 0.0
	var normal: float = bond.add_bond("food_given")
	_assert_true(is_equal_approx(normal, 4.0), "normal_feed_4")
	var bond2 = BondSystemScript.new()  # 新实例避免递减干扰
	add_child(bond2)
	var bonus: float = bond2.add_bond("food_given", -1.0, 1.5)
	_assert_true(is_equal_approx(bonus, 6.0), "hungry_feed_6")
	bond.queue_free()
	bond2.queue_free()
```

（`var bond2` 需显式类型或直接 `var b2 := BondSystemScript.new()`；执行时写对语法。）

- [x] **Step 2: bond_system.add_bond 加 multiplier 参数**

```gdscript
func add_bond(interaction_type: String, amount: float = -1.0, multiplier: float = 1.0) -> float:
	# ...原逻辑，gain 计算处改为：
	gain = (BOND_GAINS[interaction_type] if amount < 0.0 else amount) * multiplier
```

cat.gd：`_on_item_reaction`/喂食链路处，`add_bond("food_given", -1.0, 1.5 if behavior_system.energy < 40.0 else 1.0)`；发信号 `signal bond_gained(amount: float)`（add_bond 返回 >0 时 emit）。

- [x] **Step 3: 气泡小提示 + 设置面板进度条**

smart_line_bubble.gd 加：

```gdscript
func show_gain_tip(amount: float, anchor: Vector2) -> void:
	## 轻量亲密度提示：右上角小字浮层 2 秒（复用主气泡定位，不排队）
	show_line("亲密度 +%.0f" % amount, anchor + Vector2(40, -30), _last_viewport_size)
	# 简化实现：直接 show_line 复用；若主气泡正显示则跳过（is_visible_to_user 判定）
```

main.gd：cat.bond_gained.connect → `smart_line_bubble.show_gain_tip(...)`（气泡占用时丢弃）。

settings_panel：VBoxContainer 末尾加 BondProgressBar（ProgressBar）+ BondLevelLabel（Label）。settings_panel.gd：

```gdscript
@onready var bond_progress_bar: ProgressBar = $ScrollContainer/VBoxContainer/BondProgressBar
@onready var bond_level_label: Label = $ScrollContainer/VBoxContainer/BondLevelLabel

func refresh_bond_ui() -> void:
	var main = get_tree().get_root().get_node_or_null("Main")
	if not main or not main.cat or not main.cat.bond_system:
		return
	var bs = main.cat.bond_system
	var info: Dictionary = bs.get_level_info()
	var next: Dictionary = bs.get_next_level_info()
	bond_level_label.text = "亲密度 Lv.%d「%s」" % [bs.get_level(), info.get("title", "")]
	if next.is_empty():
		bond_progress_bar.value = 100
	else:
		bond_progress_bar.value = bs.get_progress() * 100.0
		bond_level_label.text += "　下级解锁：%s" % String(next.get("unlock_breed", next.get("unlock_anim", "")))
```

`_on_visibility_changed`（面板打开）时调用 `refresh_bond_ui()`；cat.bond_changed 信号在面板打开期间驱动刷新（main 转发或面板每秒 timer 轮询——选轮询，`timed_hide_update_timer` 同款 Timer 模式，1 秒周期，visible 才刷）。

- [x] **Step 4: 跑测试 + Commit**

```bash
timeout 90 "$GODOT" --headless --path . res://tests/test_bond_system.tscn
git add cat.gd components/bond_system.gd components/ui/smart_line_bubble.gd main.gd settings_panel.gd settings_panel.tscn tests/test_bond_system.gd
git commit -m "feat: 亲密度反馈钩子与进度条饿猫加成"
```

---

### Task 19: P5c 收尾 + P5 总验收

- [x] **Step 1: 全部 10+2 套测试跑绿**（九套既有 + test_presence_level + test_bond_system + test_first_guide），断言总数对比基线只增不减
- [x] **Step 2: MCP 端到端（P5c 验收线）**：
  - 备份并清空存档（`cp save_data.cfg 备份` 后删）→ 启动 → 30 秒后引导气泡出现 → 完成一次撸猫 → step2 气泡 → 投食 → 引导完成标记落档（检查 save_data.cfg `first_interaction_done=true`）
  - 二次启动 → 不重复引导
  - 模拟 30 分钟无互动（改 `_last_interaction_unix` 或等真实时长不现实——用 debug 注入：临时设置 `context_collector._last_interaction_unix = now - 2000` 跑 1 决策周期）→ 主动邀请触发一次
  - 亲密度进度条：设置面板打开 → 进度可见；撸猫 → 面板数值变化；「亲密度 +N」气泡出现
  - 还原存档备份
- [x] **Step 3: P5 总验收对照设计文档验收表逐条核对**，证据写进本文件
- [x] **Step 4: 更新 `docs/plans/2026-08-17-smart-companion-redesign.md` 追加 P5 完成记录**（对齐 P1-P4 格式），commit `docs: P5 完成记录`

---

## 附：执行提示

- P5a 的 5 个提交是"验收既有代码"，不写新实现——发现 bug 就修（独立提交），修不动记已知问题
- P5b Task 7-13 强顺序（档位键 → UI → 滑窗 → 静音 → 白名单 → LLM → 编排），Task 9/10 可互换
- randf 类意图测试必须用 override 注入（Task 16 教训内建）
- 每任务结束 `git status` 应 clean（.godot/.claude 例外）；每段收尾必须 MCP 端到端留证据
